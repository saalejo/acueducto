kind: Service
apiVersion: v1
metadata:
  name: acueducto-svc
  namespace: acueducto-sonadora
spec:
  type: ClusterIP
  selector:
      app: acueducto
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
